#/bin/bash
name=$1
dir="${HOME}/work/puppet/test_data/${name}c"
#puppetd --no-daemonize --debug --trace --verbose --confdir=${dir} --vardir=${dir} --server puppetmaster $@
puppetd --test --confdir=${dir} --vardir=${dir} --server puppetmaster $@
